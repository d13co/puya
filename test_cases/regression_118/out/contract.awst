contract Contract
{
  abimethod verify(values: algopy.arc4.DynamicArray[algopy.arc4.UInt256]): arc4.tuple<arc4.bool,arc4.string>
  {
    val1: algopy.arc4.Bool = arc4_encode(reinterpret_cast<bool>(txn<NumAppArgs>()), algopy.arc4.Bool)
    if (extract_uint16(values, 0u) != 2u) {
      return arc4_encode((val1, arc4_encode('', algopy.arc4.String)), arc4.tuple<arc4.bool,arc4.string>)
    }
    return arc4_encode((val1, arc4_encode('', algopy.arc4.String)), arc4.tuple<arc4.bool,arc4.string>)
  }
}